@using ContactList.Entities
@model ContactList.Models.HomePageModel

@{
    ViewData["Title"] = "Home Page";
}

<link rel="stylesheet" href="~/css/site.css" asp-append-version="true"/>

<div id="PlaceHolderForElement"></div>

<div class="hero">
    <div class="content">
        <button type="button" class="btn btn-outline-light" data-toggle="ajax-modal" data-target="#addContact"
                data-url="@Url.Action("Create")">
            Add Contact
        </button>

        @if (Model!.Contacts.Count != 0)
        {
            <table class="table table-hover table-dark">
                <thead>
                <tr>
                    <th scope="col">№</th>
                    <th scope="col">Name</th>
                    <th scope="col">Phone</th>
                    <th scope="col">Job</th>
                    <th scope="col">BDay</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var contact in Model.Contacts)
                {
                    <tr>
                        <th scope="row">@contact.Id.ToString()</th>
                        <td>@contact.Name</td>
                        <td>@contact.MobilePhone</td>
                        <td>@contact.JobTitle</td>
                        <td>@contact.BirthDate.ToString()</td>
                    </tr>
                }

                </tbody>
            </table>
        }
        else
        {
            <p>NO CONTACTS</p>
        }
    </div>
</div>